# Allow postinstall scripts that download prebuilt binaries, otherwise the
# Vercel build runner skips them (ffmpeg-static extracts its ffmpeg binary,
# esbuild fetches its native binary). Without ffmpeg-static's binary the
# /api/og-image video-poster extraction fails and video shares fall back white.
allow-scripts=ffmpeg-static,esbuild
